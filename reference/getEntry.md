# Get Entry

Get all list elements "entry" of a list of models

## Usage

``` r
getEntry(modelList, entry)
```

## Arguments

- modelList:

  (list) of saved model objects

- entry:

  (character) name of the list element, e.g. "fit"
