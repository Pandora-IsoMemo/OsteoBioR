# Extract Saved Models

Extract Saved Models

## Usage

``` r
extractSavedModels(upload)
```

## Arguments

- upload:

  (list) list of saved model objects with entries "data", "inputs" and
  "model"
