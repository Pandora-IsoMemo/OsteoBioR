# Get Seed

Get Seed

## Usage

``` r
getSeed(fixSeed, seedValue)
```

## Arguments

- fixSeed:

  (logical) if TRUE use fixed ssed, if FALSE use random seed when
  fitting the model

- seedValue:

  (numeric) value for seed if `fixSeed == TRUE`
