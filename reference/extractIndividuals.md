# Extract Individuals

Extract Individuals

## Usage

``` r
extractIndividuals(matrix, indVar)
```

## Arguments

- matrix:

  (matrix) matrix of data

- indVar:

  (character) name if individuals column or empty if rownames

## Value

(character) vector of individuals or row names with individuals
